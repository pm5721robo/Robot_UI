[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python3 -m venv /app/venv && /app/venv/bin/pip install -r cloud/requirements.txt"]

[start]
cmd = "cd cloud && /app/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"